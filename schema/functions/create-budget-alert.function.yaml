name: createBudgetAlert
description: Create a new BudgetAlert
inputs:
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: BudgetAlert
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
