name: updateOrganization
description: Update Organization
inputs:
  - name: id
    type: string
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: Organization
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
