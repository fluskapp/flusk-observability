name: createRoutingDecision
description: Create RoutingDecision
inputs:
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: RoutingDecision
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
