name: findOrganizationById
description: Find Organization by ID
inputs:
  - name: id
    type: string
  - name: db
    type: Database
output:
  type: Organization
steps:
  - id: result
    action: assign
    value: $id
  - id: done
    action: return
    value: $result
