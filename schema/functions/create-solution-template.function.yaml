name: createSolutionTemplate
description: Create a new SolutionTemplate
inputs:
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: SolutionTemplate
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
