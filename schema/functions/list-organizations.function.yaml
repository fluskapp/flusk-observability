name: listOrganizations
description: List all Organization records
inputs:
  - name: db
    type: Database
output:
  type: json
steps:
  - id: result
    action: assign
    value: []
  - id: done
    action: return
    value: $result
