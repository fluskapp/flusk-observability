name: createCostReport
description: Create CostReport
inputs:
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: CostReport
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
