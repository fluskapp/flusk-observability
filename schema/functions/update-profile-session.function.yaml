name: updateProfileSession
description: Update ProfileSession
inputs:
  - name: id
    type: string
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: ProfileSession
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
