name: createInsight
description: Create Insight
inputs:
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: Insight
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
