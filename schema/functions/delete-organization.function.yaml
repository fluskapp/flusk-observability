name: deleteOrganization
description: Delete Organization
inputs:
  - name: id
    type: string
  - name: db
    type: Database
output:
  type: boolean
steps:
  - id: result
    action: assign
    value: true
  - id: done
    action: return
    value: $result
