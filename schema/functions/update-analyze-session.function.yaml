name: updateAnalyzeSession
description: Update AnalyzeSession
inputs:
  - name: id
    type: string
  - name: data
    type: json
  - name: db
    type: Database
output:
  type: AnalyzeSession
steps:
  - id: result
    action: assign
    value: $data
  - id: done
    action: return
    value: $result
