// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { partnersRoutes } from '../../src/routes/partners.routes.js';

describe('partners routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(partnersRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/partners', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/partners',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/partners/register', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/partners/register',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/partners/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/partners/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('PUT /api/partners/:id', async () => {
    const res = await app.inject({
      method: 'PUT',
      url: '/api/partners/test-id',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/partners/:id/approve', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/partners/test-id/approve',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('DELETE /api/partners/:id', async () => {
    const res = await app.inject({
      method: 'DELETE',
      url: '/api/partners/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
