// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import { analyzeSessionsRoutes } from '../../src/routes/analyzeSessions.routes.js';

describe('analyzeSessions routes', () => {
  const app = Fastify();

  beforeAll(async () => {
    await app.register(analyzeSessionsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
  });

  it('GET /api/analyze-sessions', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/analyze-sessions',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/analyze-sessions/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/analyze-sessions/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/analyze-sessions', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/analyze-sessions',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
