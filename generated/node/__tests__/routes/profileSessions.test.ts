// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { profileSessionsRoutes } from '../../src/routes/profileSessions.routes.js';

describe('profileSessions routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(profileSessionsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/profile-sessions', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/profile-sessions',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/profile-sessions/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/profile-sessions/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/profile-sessions/start', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/profile-sessions/start',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/profile-sessions/:id/stop', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/profile-sessions/test-id/stop',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/profile-sessions/:id/patterns', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/profile-sessions/test-id/patterns',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
