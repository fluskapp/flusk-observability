// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { patternsRoutes } from '../../src/routes/patterns.routes.js';

describe('patterns routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(patternsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/patterns', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/patterns',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/patterns/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/patterns/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/patterns/detect', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/patterns/detect',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
