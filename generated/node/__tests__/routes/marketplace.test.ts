// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { marketplaceRoutes } from '../../src/routes/marketplace.routes.js';

describe('marketplace routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(marketplaceRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/marketplace', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/marketplace',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/marketplace/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/marketplace/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/marketplace/install', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/marketplace/install',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
