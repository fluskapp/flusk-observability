// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { costEventsRoutes } from '../../src/routes/costEvents.routes.js';

describe('costEvents routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(costEventsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/cost-events/stream', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/cost-events/stream',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/cost-events/daily', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/cost-events/daily',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/cost-events/monthly', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/cost-events/monthly',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
