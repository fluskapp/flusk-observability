// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { driftDetectionsRoutes } from '../../src/routes/driftDetections.routes.js';

describe('driftDetections routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(driftDetectionsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/drift-detections', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/drift-detections',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/drift-detections/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/drift-detections/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('PUT /api/drift-detections/:id', async () => {
    const res = await app.inject({
      method: 'PUT',
      url: '/api/drift-detections/test-id',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('DELETE /api/drift-detections/:id', async () => {
    const res = await app.inject({
      method: 'DELETE',
      url: '/api/drift-detections/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/drift-detections/scan', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/drift-detections/scan',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
