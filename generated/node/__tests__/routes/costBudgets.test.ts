// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { costBudgetsRoutes } from '../../src/routes/costBudgets.routes.js';

describe('costBudgets routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(costBudgetsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/cost-budgets', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/cost-budgets',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/cost-budgets', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/cost-budgets',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('PUT /api/cost-budgets/:id', async () => {
    const res = await app.inject({
      method: 'PUT',
      url: '/api/cost-budgets/test-id',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('DELETE /api/cost-budgets/:id', async () => {
    const res = await app.inject({
      method: 'DELETE',
      url: '/api/cost-budgets/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/cost-budgets/check/:dimension/:value', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/cost-budgets/check/test-id/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
