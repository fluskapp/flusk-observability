// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import { dashboardWidgetsRoutes } from '../../src/routes/dashboardWidgets.routes.js';

describe('dashboardWidgets routes', () => {
  const app = Fastify();

  beforeAll(async () => {
    await app.register(dashboardWidgetsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
  });

  it('GET /api/dashboard-widgets', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/dashboard-widgets',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/dashboard-widgets', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/dashboard-widgets',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/dashboard-widgets/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/dashboard-widgets/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('PUT /api/dashboard-widgets/:id', async () => {
    const res = await app.inject({
      method: 'PUT',
      url: '/api/dashboard-widgets/test-id',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('DELETE /api/dashboard-widgets/:id', async () => {
    const res = await app.inject({
      method: 'DELETE',
      url: '/api/dashboard-widgets/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
