// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { costReportsRoutes } from '../../src/routes/costReports.routes.js';

describe('costReports routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(costReportsRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('GET /api/cost-reports', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/cost-reports',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('POST /api/cost-reports/generate', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/cost-reports/generate',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/cost-reports/:id', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/cost-reports/test-id',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
