// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AiUsageMetricRepository } from '../../src/repositories/aiusagemetric.repository.js';

describe('AiUsageMetricRepository', () => {
  let db: Database.Database;
  let repo: AiUsageMetricRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE ai_usage_metrics (id TEXT PRIMARY KEY, user_id TEXT NOT NULL, tool_name TEXT NOT NULL, metric_type TEXT NOT NULL, value REAL NOT NULL, period TEXT NOT NULL, period_type TEXT NOT NULL, metadata TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AiUsageMetricRepository(db);
  });

  const testData = () => ({
    userId: 'test-userId',
    toolName: 'test-toolName',
    metricType: 'prompt-count',
    value: 3.14,
    period: 'test-period',
    periodType: 'daily',
    metadata: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { userId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
