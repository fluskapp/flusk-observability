// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { PerformancePatternRepository } from '../../src/repositories/performancepattern.repository.js';

describe('PerformancePatternRepository', () => {
  let db: Database.Database;
  let repo: PerformancePatternRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE performance_patterns (id TEXT PRIMARY KEY, pattern_type TEXT NOT NULL, function_name TEXT NOT NULL, file_path TEXT NOT NULL, line_number INTEGER NOT NULL, self_time REAL NOT NULL, total_time REAL NOT NULL, call_count INTEGER NOT NULL, heap_delta INTEGER NOT NULL, severity TEXT NOT NULL, profile_session_id TEXT NOT NULL, recommendation TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new PerformancePatternRepository(db);
  });

  const testData = () => ({
    patternType: 'hot_function',
    functionName: 'test-functionName',
    filePath: 'test-filePath',
    lineNumber: 42,
    selfTime: 3.14,
    totalTime: 3.14,
    callCount: 42,
    heapDelta: 42,
    severity: 'critical',
    profileSessionId: 'test-profileSessionId',
    recommendation: 'test-recommendation',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { patternType: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
