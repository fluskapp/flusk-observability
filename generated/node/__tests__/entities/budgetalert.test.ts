// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { BudgetAlertRepository } from '../../src/repositories/budgetalert.repository.js';

describe('BudgetAlertRepository', () => {
  let db: Database.Database;
  let repo: BudgetAlertRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE budget_alerts (id TEXT PRIMARY KEY, name TEXT NOT NULL, budget_type TEXT NOT NULL, threshold_usd REAL NOT NULL, warning_percent INTEGER NOT NULL, current_spend_usd REAL NOT NULL, enabled INTEGER NOT NULL, auto_pause INTEGER NOT NULL, last_triggered_at TEXT NOT NULL, agent_filter TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new BudgetAlertRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    budgetType: 'daily',
    thresholdUsd: 3.14,
    warningPercent: 42,
    currentSpendUsd: 3.14,
    enabled: true,
    autoPause: true,
    lastTriggeredAt: 'test-lastTriggeredAt',
    agentFilter: 'test-agentFilter',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
