// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { OptimizationRepository } from '../../src/repositories/optimization.repository.js';

describe('OptimizationRepository', () => {
  let db: Database.Database;
  let repo: OptimizationRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE optimizations (id TEXT PRIMARY KEY, optimization_type TEXT NOT NULL, title TEXT NOT NULL, description TEXT NOT NULL, code_before TEXT NOT NULL, code_after TEXT NOT NULL, file_path TEXT NOT NULL, line_number INTEGER NOT NULL, estimated_savings_usd REAL NOT NULL, status TEXT NOT NULL, analyze_session_id TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new OptimizationRepository(db);
  });

  const testData = () => ({
    optimizationType: 'caching',
    title: 'test-title',
    description: 'test-description',
    codeBefore: 'test-codeBefore',
    codeAfter: 'test-codeAfter',
    filePath: 'test-filePath',
    lineNumber: 42,
    estimatedSavingsUsd: 3.14,
    status: 'pending',
    analyzeSessionId: 'test-analyzeSessionId',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { optimizationType: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
