// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { ModelPerformanceRepository } from '../../src/repositories/modelperformance.repository.js';

describe('ModelPerformanceRepository', () => {
  let db: Database.Database;
  let repo: ModelPerformanceRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE model_performances (id TEXT PRIMARY KEY, model TEXT NOT NULL, provider TEXT NOT NULL, prompt_category TEXT NOT NULL, call_count INTEGER NOT NULL, avg_cost_usd REAL NOT NULL, avg_duration_ms REAL NOT NULL, avg_tokens REAL NOT NULL, quality_score REAL NOT NULL, error_rate REAL NOT NULL, p50duration_ms INTEGER NOT NULL, p99duration_ms INTEGER NOT NULL, last_updated_at TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new ModelPerformanceRepository(db);
  });

  const testData = () => ({
    model: 'test-model',
    provider: 'openai',
    promptCategory: 'test-promptCategory',
    callCount: 42,
    avgCostUsd: 3.14,
    avgDurationMs: 3.14,
    avgTokens: 3.14,
    qualityScore: 3.14,
    errorRate: 3.14,
    p50DurationMs: 42,
    p99DurationMs: 42,
    lastUpdatedAt: 'test-lastUpdatedAt',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { model: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
