// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { RoutingRuleRepository } from '../../src/repositories/routingrule.repository.js';

describe('RoutingRuleRepository', () => {
  let db: Database.Database;
  let repo: RoutingRuleRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE routing_rules (id TEXT PRIMARY KEY, name TEXT NOT NULL, prompt_category TEXT NOT NULL, min_quality_score REAL NOT NULL, max_cost_usd REAL NOT NULL, max_duration_ms INTEGER NOT NULL, preferred_models TEXT NOT NULL, excluded_models TEXT NOT NULL, enabled INTEGER NOT NULL, priority INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new RoutingRuleRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    promptCategory: 'test-promptCategory',
    minQualityScore: 3.14,
    maxCostUsd: 3.14,
    maxDurationMs: 42,
    preferredModels: { key: 'value' },
    excludedModels: { key: 'value' },
    enabled: true,
    priority: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
