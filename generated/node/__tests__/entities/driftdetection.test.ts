// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { DriftDetectionRepository } from '../../src/repositories/driftdetection.repository.js';

describe('DriftDetectionRepository', () => {
  let db: Database.Database;
  let repo: DriftDetectionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE drift_detections (id TEXT PRIMARY KEY, agent_label TEXT NOT NULL, detection_type TEXT NOT NULL, severity TEXT NOT NULL, baseline_hash TEXT NOT NULL, current_hash TEXT NOT NULL, drift_score REAL NOT NULL, sample_count INTEGER NOT NULL, baseline_period TEXT NOT NULL, current_period TEXT NOT NULL, details TEXT NOT NULL, status TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new DriftDetectionRepository(db);
  });

  const testData = () => ({
    agentLabel: 'test-agentLabel',
    detectionType: 'output-drift',
    severity: 'critical',
    baselineHash: 'test-baselineHash',
    currentHash: 'test-currentHash',
    driftScore: 3.14,
    sampleCount: 42,
    baselinePeriod: 'test-baselinePeriod',
    currentPeriod: 'test-currentPeriod',
    details: { key: 'value' },
    status: 'active',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { agentLabel: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
