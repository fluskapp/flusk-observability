// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { DelusionDetectionRepository } from '../../src/repositories/delusiondetection.repository.js';

describe('DelusionDetectionRepository', () => {
  let db: Database.Database;
  let repo: DelusionDetectionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE delusion_detections (id TEXT PRIMARY KEY, agent_label TEXT NOT NULL, llm_call_id TEXT NOT NULL, delusion_type TEXT NOT NULL, confidence REAL NOT NULL, actual_accuracy REAL NOT NULL, divergence_score REAL NOT NULL, input_sample TEXT NOT NULL, output_sample TEXT NOT NULL, explanation TEXT NOT NULL, verified INTEGER NOT NULL, status TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new DelusionDetectionRepository(db);
  });

  const testData = () => ({
    agentLabel: 'test-agentLabel',
    llmCallId: 'test-llmCallId',
    delusionType: 'hallucination',
    confidence: 3.14,
    actualAccuracy: 3.14,
    divergenceScore: 3.14,
    inputSample: 'test-inputSample',
    outputSample: 'test-outputSample',
    explanation: 'test-explanation',
    verified: true,
    status: 'detected',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { agentLabel: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
