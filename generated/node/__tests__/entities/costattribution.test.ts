// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { CostAttributionRepository } from '../../src/repositories/costattribution.repository.js';

describe('CostAttributionRepository', () => {
  let db: Database.Database;
  let repo: CostAttributionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE cost_attributions (id TEXT PRIMARY KEY, llm_call_id TEXT NOT NULL, tag_key TEXT NOT NULL, tag_value TEXT NOT NULL, cost_usd INTEGER NOT NULL, input_tokens INTEGER NOT NULL, output_tokens INTEGER NOT NULL, model TEXT NOT NULL, provider TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new CostAttributionRepository(db);
  });

  const testData = () => ({
    llmCallId: 'test-llmCallId',
    tagKey: 'test-tagKey',
    tagValue: 'test-tagValue',
    costUsd: 42,
    inputTokens: 42,
    outputTokens: 42,
    model: 'test-model',
    provider: 'test-provider',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { llmCallId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
