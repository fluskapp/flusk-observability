// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { SolutionTemplateRepository } from '../../src/repositories/solutiontemplate.repository.js';

describe('SolutionTemplateRepository', () => {
  let db: Database.Database;
  let repo: SolutionTemplateRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE solution_templates (id TEXT PRIMARY KEY, name TEXT NOT NULL, description TEXT NOT NULL, category TEXT NOT NULL, config TEXT NOT NULL, is_public INTEGER NOT NULL, author TEXT NOT NULL, downloads INTEGER NOT NULL, rating REAL NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new SolutionTemplateRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    description: 'test-description',
    category: 'customer-support',
    config: { key: 'value' },
    isPublic: true,
    author: 'test-author',
    downloads: 42,
    rating: 3.14,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
