// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { ProfileSessionRepository } from '../../src/repositories/profilesession.repository.js';

describe('ProfileSessionRepository', () => {
  let db: Database.Database;
  let repo: ProfileSessionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE profile_sessions (id TEXT PRIMARY KEY, name TEXT NOT NULL, profile_type TEXT NOT NULL, status TEXT NOT NULL, pid INTEGER NOT NULL, duration_ms INTEGER NOT NULL, flame_graph_path TEXT NOT NULL, cpu_profile_path TEXT NOT NULL, heap_snapshot_path TEXT NOT NULL, hotspot_count INTEGER NOT NULL, peak_heap_mb REAL NOT NULL, summary TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new ProfileSessionRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    profileType: 'cpu',
    status: 'running',
    pid: 42,
    durationMs: 42,
    flameGraphPath: 'test-flameGraphPath',
    cpuProfilePath: 'test-cpuProfilePath',
    heapSnapshotPath: 'test-heapSnapshotPath',
    hotspotCount: 42,
    peakHeapMb: 3.14,
    summary: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
