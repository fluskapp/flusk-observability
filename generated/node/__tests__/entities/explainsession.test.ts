// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { ExplainSessionRepository } from '../../src/repositories/explainsession.repository.js';

describe('ExplainSessionRepository', () => {
  let db: Database.Database;
  let repo: ExplainSessionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE explain_sessions (id TEXT PRIMARY KEY, query TEXT NOT NULL, status TEXT NOT NULL, model TEXT NOT NULL, response_text TEXT NOT NULL, calls_analyzed INTEGER NOT NULL, cost_of_explain REAL NOT NULL, duration_ms INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new ExplainSessionRepository(db);
  });

  const testData = () => ({
    query: 'test-query',
    status: 'running',
    model: 'test-model',
    responseText: 'test-responseText',
    callsAnalyzed: 42,
    costOfExplain: 3.14,
    durationMs: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { query: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
