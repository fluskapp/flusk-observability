// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { DashboardWidgetRepository } from '../../src/repositories/dashboardwidget.repository.js';

describe('DashboardWidgetRepository', () => {
  let db: Database.Database;
  let repo: DashboardWidgetRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE dashboard_widgets (id TEXT PRIMARY KEY, dashboard_id TEXT NOT NULL, widget_type TEXT NOT NULL, title TEXT NOT NULL, query TEXT NOT NULL, position TEXT NOT NULL, config TEXT NOT NULL, refresh_override INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new DashboardWidgetRepository(db);
  });

  const testData = () => ({
    dashboardId: 'test-dashboardId',
    widgetType: 'line-chart',
    title: 'test-title',
    query: { key: 'value' },
    position: { key: 'value' },
    config: { key: 'value' },
    refreshOverride: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { dashboardId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
