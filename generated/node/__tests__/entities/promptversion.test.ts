// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { PromptVersionRepository } from '../../src/repositories/promptversion.repository.js';

describe('PromptVersionRepository', () => {
  let db: Database.Database;
  let repo: PromptVersionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE prompt_versions (id TEXT PRIMARY KEY, template_id TEXT NOT NULL, version INTEGER NOT NULL, prompt_text TEXT NOT NULL, model TEXT NOT NULL, call_count INTEGER NOT NULL, avg_cost_usd REAL NOT NULL, avg_duration_ms REAL NOT NULL, avg_quality_score REAL NOT NULL, is_active INTEGER NOT NULL, traffic_percent INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new PromptVersionRepository(db);
  });

  const testData = () => ({
    templateId: 'test-templateId',
    version: 42,
    promptText: 'test-promptText',
    model: 'test-model',
    callCount: 42,
    avgCostUsd: 3.14,
    avgDurationMs: 3.14,
    avgQualityScore: 3.14,
    isActive: true,
    trafficPercent: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { templateId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
