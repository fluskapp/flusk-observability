// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { CostBudgetRepository } from '../../src/repositories/costbudget.repository.js';

describe('CostBudgetRepository', () => {
  let db: Database.Database;
  let repo: CostBudgetRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE cost_budgets (id TEXT PRIMARY KEY, tag_key TEXT NOT NULL, tag_value TEXT NOT NULL, limit_usd INTEGER NOT NULL, period TEXT NOT NULL, alert_at INTEGER NOT NULL, action TEXT NOT NULL, enabled INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new CostBudgetRepository(db);
  });

  const testData = () => ({
    tagKey: 'test-tagKey',
    tagValue: 'test-tagValue',
    limitUsd: 42,
    period: 'daily',
    alertAt: 42,
    action: 'alert',
    enabled: true,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { tagKey: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
