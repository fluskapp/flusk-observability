// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { SolutionRunRepository } from '../../src/repositories/solutionrun.repository.js';

describe('SolutionRunRepository', () => {
  let db: Database.Database;
  let repo: SolutionRunRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE solution_runs (id TEXT PRIMARY KEY, solution_id TEXT NOT NULL, organization_id TEXT NOT NULL, input TEXT NOT NULL, output TEXT NOT NULL, status TEXT NOT NULL, cost REAL NOT NULL, duration_ms INTEGER NOT NULL, model_used TEXT NOT NULL, tokens_in INTEGER NOT NULL, tokens_out INTEGER NOT NULL, error TEXT NOT NULL, trace_id TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new SolutionRunRepository(db);
  });

  const testData = () => ({
    solutionId: 'test-solutionId',
    organizationId: 'test-organizationId',
    input: { key: 'value' },
    output: { key: 'value' },
    status: 'running',
    cost: 3.14,
    durationMs: 42,
    modelUsed: 'test-modelUsed',
    tokensIn: 42,
    tokensOut: 42,
    error: 'test-error',
    traceId: 'test-traceId',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { solutionId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
