// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { CodeScanResultRepository } from '../../src/repositories/codescanresult.repository.js';

describe('CodeScanResultRepository', () => {
  let db: Database.Database;
  let repo: CodeScanResultRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE code_scan_results (id TEXT PRIMARY KEY, project_path TEXT NOT NULL, scan_type TEXT NOT NULL, total_llm_calls INTEGER NOT NULL, total_providers INTEGER NOT NULL, total_models INTEGER NOT NULL, call_sites TEXT NOT NULL, recommendations TEXT NOT NULL, risk_score REAL NOT NULL, scan_duration INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new CodeScanResultRepository(db);
  });

  const testData = () => ({
    projectPath: 'test-projectPath',
    scanType: 'static',
    totalLlmCalls: 42,
    totalProviders: 42,
    totalModels: 42,
    callSites: { key: 'value' },
    recommendations: { key: 'value' },
    riskScore: 3.14,
    scanDuration: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { projectPath: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
