// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { ConversionRepository } from '../../src/repositories/conversion.repository.js';

describe('ConversionRepository', () => {
  let db: Database.Database;
  let repo: ConversionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE conversions (id TEXT PRIMARY KEY, conversion_type TEXT NOT NULL, source_model TEXT NOT NULL, target_model TEXT NOT NULL, prompt_hash TEXT NOT NULL, prompt_category TEXT NOT NULL, estimated_savings_usd REAL NOT NULL, confidence REAL NOT NULL, status TEXT NOT NULL, applied_at TEXT NOT NULL, metadata TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new ConversionRepository(db);
  });

  const testData = () => ({
    conversionType: 'cache',
    sourceModel: 'test-sourceModel',
    targetModel: 'test-targetModel',
    promptHash: 'test-promptHash',
    promptCategory: 'test-promptCategory',
    estimatedSavingsUsd: 3.14,
    confidence: 3.14,
    status: 'pending',
    appliedAt: 'test-appliedAt',
    metadata: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { conversionType: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
