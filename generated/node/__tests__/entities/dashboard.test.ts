// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { DashboardRepository } from '../../src/repositories/dashboard.repository.js';

describe('DashboardRepository', () => {
  let db: Database.Database;
  let repo: DashboardRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE dashboards (id TEXT PRIMARY KEY, name TEXT NOT NULL, description TEXT NOT NULL, layout TEXT NOT NULL, filters TEXT NOT NULL, refresh_interval INTEGER NOT NULL, is_default INTEGER NOT NULL, owner TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new DashboardRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    description: 'test-description',
    layout: { key: 'value' },
    filters: { key: 'value' },
    refreshInterval: 42,
    isDefault: true,
    owner: 'test-owner',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
