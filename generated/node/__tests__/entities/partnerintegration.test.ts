// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { PartnerIntegrationRepository } from '../../src/repositories/partnerintegration.repository.js';

describe('PartnerIntegrationRepository', () => {
  let db: Database.Database;
  let repo: PartnerIntegrationRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE partner_integrations (id TEXT PRIMARY KEY, partner_id TEXT NOT NULL, name TEXT NOT NULL, type TEXT NOT NULL, config TEXT NOT NULL, status TEXT NOT NULL, endpoint_url TEXT NOT NULL, documentation TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new PartnerIntegrationRepository(db);
  });

  const testData = () => ({
    partnerId: 'test-partnerId',
    name: 'test-name',
    type: 'model-provider',
    config: { key: 'value' },
    status: 'active',
    endpointUrl: 'test-endpointUrl',
    documentation: 'test-documentation',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { partnerId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
