// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { PartnerRepository } from '../../src/repositories/partner.repository.js';

describe('PartnerRepository', () => {
  let db: Database.Database;
  let repo: PartnerRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE partners (id TEXT PRIMARY KEY, name TEXT NOT NULL, email TEXT NOT NULL, website TEXT NOT NULL, status TEXT NOT NULL, api_key TEXT NOT NULL, tier TEXT NOT NULL, organization_id TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new PartnerRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    email: 'test-email',
    website: 'test-website',
    status: 'pending',
    apiKey: 'test-apiKey',
    tier: 'free',
    organizationId: 'test-organizationId',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
