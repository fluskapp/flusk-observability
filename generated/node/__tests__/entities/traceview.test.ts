// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { TraceViewRepository } from '../../src/repositories/traceview.repository.js';

describe('TraceViewRepository', () => {
  let db: Database.Database;
  let repo: TraceViewRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE trace_views (id TEXT PRIMARY KEY, trace_id TEXT NOT NULL, view_type TEXT NOT NULL, total_duration INTEGER NOT NULL, total_cost REAL NOT NULL, span_count INTEGER NOT NULL, critical_path TEXT NOT NULL, annotations TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new TraceViewRepository(db);
  });

  const testData = () => ({
    traceId: 'test-traceId',
    viewType: 'waterfall',
    totalDuration: 42,
    totalCost: 3.14,
    spanCount: 42,
    criticalPath: { key: 'value' },
    annotations: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { traceId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
