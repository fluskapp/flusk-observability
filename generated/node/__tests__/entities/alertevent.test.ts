// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AlertEventRepository } from '../../src/repositories/alertevent.repository.js';

describe('AlertEventRepository', () => {
  let db: Database.Database;
  let repo: AlertEventRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE alert_events (id TEXT PRIMARY KEY, alert_type TEXT NOT NULL, severity TEXT NOT NULL, title TEXT NOT NULL, message TEXT NOT NULL, channel_name TEXT NOT NULL, delivered INTEGER NOT NULL, acknowledged_at TEXT NOT NULL, metadata TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AlertEventRepository(db);
  });

  const testData = () => ({
    alertType: 'budget_exceeded',
    severity: 'critical',
    title: 'test-title',
    message: 'test-message',
    channelName: 'test-channelName',
    delivered: true,
    acknowledgedAt: 'test-acknowledgedAt',
    metadata: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { alertType: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
