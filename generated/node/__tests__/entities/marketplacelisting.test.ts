// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { MarketplaceListingRepository } from '../../src/repositories/marketplacelisting.repository.js';

describe('MarketplaceListingRepository', () => {
  let db: Database.Database;
  let repo: MarketplaceListingRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE marketplace_listings (id TEXT PRIMARY KEY, partner_id TEXT NOT NULL, integration_id TEXT NOT NULL, name TEXT NOT NULL, description TEXT NOT NULL, category TEXT NOT NULL, pricing TEXT NOT NULL, featured INTEGER NOT NULL, rating REAL NOT NULL, install_count INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new MarketplaceListingRepository(db);
  });

  const testData = () => ({
    partnerId: 'test-partnerId',
    integrationId: 'test-integrationId',
    name: 'test-name',
    description: 'test-description',
    category: 'test-category',
    pricing: { key: 'value' },
    featured: true,
    rating: 3.14,
    installCount: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { partnerId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
