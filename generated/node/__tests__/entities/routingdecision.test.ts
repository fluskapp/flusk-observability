// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { RoutingDecisionRepository } from '../../src/repositories/routingdecision.repository.js';

describe('RoutingDecisionRepository', () => {
  let db: Database.Database;
  let repo: RoutingDecisionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE routing_decisions (id TEXT PRIMARY KEY, routing_rule_id TEXT NOT NULL, prompt_category TEXT NOT NULL, requested_model TEXT NOT NULL, selected_model TEXT NOT NULL, was_rerouted INTEGER NOT NULL, savings_usd REAL NOT NULL, quality_delta REAL NOT NULL, latency_delta INTEGER NOT NULL, llm_call_id TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new RoutingDecisionRepository(db);
  });

  const testData = () => ({
    routingRuleId: 'test-routingRuleId',
    promptCategory: 'test-promptCategory',
    requestedModel: 'test-requestedModel',
    selectedModel: 'test-selectedModel',
    wasRerouted: true,
    savingsUsd: 3.14,
    qualityDelta: 3.14,
    latencyDelta: 42,
    llmCallId: 'test-llmCallId',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { routingRuleId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
