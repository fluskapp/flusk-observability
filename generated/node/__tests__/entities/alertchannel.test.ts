// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AlertChannelRepository } from '../../src/repositories/alertchannel.repository.js';

describe('AlertChannelRepository', () => {
  let db: Database.Database;
  let repo: AlertChannelRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE alert_channels (id TEXT PRIMARY KEY, name TEXT NOT NULL, channel_type TEXT NOT NULL, config TEXT NOT NULL, enabled INTEGER NOT NULL, severity_filter TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AlertChannelRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    channelType: 'pagerduty',
    config: { key: 'value' },
    enabled: true,
    severityFilter: 'critical',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
