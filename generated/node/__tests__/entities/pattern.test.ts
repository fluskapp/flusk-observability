// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { PatternRepository } from '../../src/repositories/pattern.repository.js';

describe('PatternRepository', () => {
  let db: Database.Database;
  let repo: PatternRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE patterns (id TEXT PRIMARY KEY, prompt_hash TEXT NOT NULL, model TEXT NOT NULL, occurrences INTEGER NOT NULL, total_cost_usd REAL NOT NULL, avg_tokens REAL NOT NULL, first_seen_at TEXT NOT NULL, last_seen_at TEXT NOT NULL, sample_prompt TEXT NOT NULL, recommendation TEXT NOT NULL, agent_name TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new PatternRepository(db);
  });

  const testData = () => ({
    promptHash: 'test-promptHash',
    model: 'test-model',
    occurrences: 42,
    totalCostUsd: 3.14,
    avgTokens: 3.14,
    firstSeenAt: 'test-firstSeenAt',
    lastSeenAt: 'test-lastSeenAt',
    samplePrompt: 'test-samplePrompt',
    recommendation: 'cache',
    agentName: 'test-agentName',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { promptHash: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
