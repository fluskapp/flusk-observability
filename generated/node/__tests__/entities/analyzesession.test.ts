// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AnalyzeSessionRepository } from '../../src/repositories/analyzesession.repository.js';

describe('AnalyzeSessionRepository', () => {
  let db: Database.Database;
  let repo: AnalyzeSessionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE analyze_sessions (id TEXT PRIMARY KEY, script_path TEXT NOT NULL, status TEXT NOT NULL, call_count INTEGER NOT NULL, total_cost_usd REAL NOT NULL, total_tokens INTEGER NOT NULL, duration_ms INTEGER NOT NULL, patterns_found INTEGER NOT NULL, insights_generated INTEGER NOT NULL, summary TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AnalyzeSessionRepository(db);
  });

  const testData = () => ({
    scriptPath: 'test-scriptPath',
    status: 'running',
    callCount: 42,
    totalCostUsd: 3.14,
    totalTokens: 42,
    durationMs: 42,
    patternsFound: 42,
    insightsGenerated: 42,
    summary: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { scriptPath: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
