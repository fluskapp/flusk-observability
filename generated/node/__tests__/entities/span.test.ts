// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { SpanRepository } from '../../src/repositories/span.repository.js';

describe('SpanRepository', () => {
  let db: Database.Database;
  let repo: SpanRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE spans (id TEXT PRIMARY KEY, trace_id TEXT NOT NULL, span_id TEXT NOT NULL, parent_span_id TEXT NOT NULL, operation_name TEXT NOT NULL, service_name TEXT NOT NULL, start_time TEXT NOT NULL, end_time TEXT NOT NULL, duration_ms INTEGER NOT NULL, kind TEXT NOT NULL, status TEXT NOT NULL, attributes TEXT NOT NULL, events TEXT NOT NULL, llm_call_id TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new SpanRepository(db);
  });

  const testData = () => ({
    traceId: 'test-traceId',
    spanId: 'test-spanId',
    parentSpanId: 'test-parentSpanId',
    operationName: 'test-operationName',
    serviceName: 'test-serviceName',
    startTime: 'test-startTime',
    endTime: 'test-endTime',
    durationMs: 42,
    kind: 'internal',
    status: 'ok',
    attributes: { key: 'value' },
    events: { key: 'value' },
    llmCallId: 'test-llmCallId',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { traceId: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
