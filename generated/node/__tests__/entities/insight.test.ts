// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { InsightRepository } from '../../src/repositories/insight.repository.js';

describe('InsightRepository', () => {
  let db: Database.Database;
  let repo: InsightRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE insights (id TEXT PRIMARY KEY, insight_type TEXT NOT NULL, title TEXT NOT NULL, description TEXT NOT NULL, estimated_savings_usd REAL NOT NULL, estimated_savings_percent REAL NOT NULL, priority TEXT NOT NULL, status TEXT NOT NULL, affected_model TEXT NOT NULL, affected_agent TEXT NOT NULL, analyze_session_id TEXT NOT NULL, evidence TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new InsightRepository(db);
  });

  const testData = () => ({
    insightType: 'cost_reduction',
    title: 'test-title',
    description: 'test-description',
    estimatedSavingsUsd: 3.14,
    estimatedSavingsPercent: 3.14,
    priority: 'high',
    status: 'new',
    affectedModel: 'test-affectedModel',
    affectedAgent: 'test-affectedAgent',
    analyzeSessionId: 'test-analyzeSessionId',
    evidence: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { insightType: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
