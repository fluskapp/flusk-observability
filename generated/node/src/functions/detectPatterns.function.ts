// @generated by flusk-lang — DO NOT EDIT

import type Database from 'better-sqlite3';
import { detectDuplicates } from './detect-duplicates.function.js';
import { createPattern } from './create-pattern.function.js';

export const detectPatterns = (db: Database.Database, start: string, end: string, minOccurrences: unknown): unknown[] => {
  const duplicates = detectDuplicates(db, start, end, minOccurrences);
  for (const item of duplicates as unknown[]) {
    try {
      createPattern(db, { promptHash: item.promptHash, model: item.model, occurrences: item.count, totalCostUsd: item.wasted_cost, firstSeenAt: start, lastSeenAt: end, recommendation: 'cache' });
    } catch (err) {
      throw err;
    }
  }
  return patterns;
};
