// @generated by flusk-lang — DO NOT EDIT

import type Database from 'better-sqlite3';
import { findProfileSessionById } from './findProfileSessionById.function.js';
import { createPerformancePattern } from './createPerformancePattern.function.js';

export const detectHotspots = (db: Database.Database, profileSessionId: string, threshold: unknown): unknown[] => {
  const session = findProfileSessionById(db, profileSessionId);
  // TODO: implement action "call" for step "patterns"
  for (const item of patterns as unknown[]) {
    try {
      createPerformancePattern(db, { patternType: item.type, functionName: item.functionName, filePath: item.filePath, selfTime: item.selfTime, totalTime: item.totalTime, severity: item.selfTime > 100 ? 'critical' : 'warning', profileSessionId: profileSessionId });
    } catch (err) {
      throw err;
    }
  }
  return saved;
};
