// @generated by flusk-lang — DO NOT EDIT

import type Database from 'better-sqlite3';
import { DashboardRepository } from '../repositories/dashboard.repository.js';
import { DashboardWidgetRepository } from '../repositories/dashboardwidget.repository.js';

export const renderDashboardData = (dashboardId: string, timeRange: Record<string, unknown>): unknown => {
  const dashboardRepo = new DashboardRepository(db);
  const dashboard = dashboardRepo.list(undefined);
  const dashboardWidgetRepo = new DashboardWidgetRepository(db);
  const widgets = dashboardWidgetRepo.list(undefined);
  for (const item of undefined as unknown[]) {
    try {
      undefined(item);
    } catch (err) {
      throw err;
    }
  }
  return { dashboard: dashboard, widgets: widgetData };
};
