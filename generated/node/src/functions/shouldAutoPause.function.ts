// @generated by flusk-lang — DO NOT EDIT

import type Database from 'better-sqlite3';
import { checkCircuit } from './check-circuit.function.js';
import { dispatchAlert } from './dispatch-alert.function.js';

export const shouldAutoPause = (db: Database.Database, config: unknown): unknown => {
  const status = checkCircuit(db, config);
  let check: unknown = undefined;
  if (status.tripped === true) {
    check = dispatchAlert(db, { alertType: 'budget_exceeded', severity: 'critical', title: 'Circuit breaker tripped', message: status.reason: spent $status.actual (limit: $status.limit) });
  }
  return false;
};
