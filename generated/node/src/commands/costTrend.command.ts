// @generated by flusk-lang — DO NOT EDIT

import { Command } from 'commander';

export const costTrendCommand = new Command('costTrend')
  .description("Show daily cost trend for a specific tag")
  .argument('[days]', "days")
  .action(async (days, options) => {
    await getCostTrend({
      dimension: options.dimension,
      value: options.value,
      from: options.from,
      to: options.to,
      db: options.db,
    });
  });
