// @generated by flusk-lang — DO NOT EDIT

import { Command } from 'commander';

export const costReportCommand = new Command('costReport')
  .description("Generate cost attribution report")
  .argument('[period]', "Report period (daily, weekly, monthly, custom)")
  .argument('[from]', "Custom period start (ISO date)")
  .argument('[to]', "Custom period end (ISO date)")
  .argument('[format]', "Output format (json, html, slack, markdown)")
  .argument('[dimension]', "Filter to specific dimension")
  .action(async (period, from, to, format, dimension, options) => {
    await generateCostReport({
      reportType: period,
      from: from,
      to: to,
      format: format,
      db: options.db,
    });
  });
