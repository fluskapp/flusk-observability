// @generated by flusk-lang — DO NOT EDIT

import { Command } from 'commander';

export const analyzeCommand = new Command('analyze')
  .description("Run cost analysis on a script — captures LLM calls, detects patterns, generates insights")
  .argument('<script>', "Path to the script to analyze")
  .argument('[depth]', "Analysis depth")
  .action(async (script, depth, options) => {
    await analyze();
  });
