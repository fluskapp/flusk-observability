// @generated by flusk-lang — DO NOT EDIT

import { Command } from 'commander';

export const historyCommand = new Command('history')
  .description("Show LLM call history")
  .argument('[limit]', "Number of calls to show")
  .argument('[model]', "Filter by model")
  .argument('[agent]', "Filter by agent")
  .argument('[since]', "Show calls since (ISO date or relative like 1h, 1d)")
  .action(async (limit, model, agent, since, options) => {
    await history();
  });
