// @generated by flusk-lang — DO NOT EDIT

import { Command } from 'commander';

export const purgeCommand = new Command('purge')
  .description("Delete old observability data")
  .argument('<older-than>', "Delete data older than (e.g. 30d, 90d, 1y)")
  .argument('[type]', "Type of data to purge")
  .argument('[dry-run]', "Show what would be deleted without deleting")
  .argument('[confirm]', "Skip confirmation prompt")
  .action(async (older-than, type, dry-run, confirm, options) => {
    await purge();
  });
