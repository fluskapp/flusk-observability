// @generated by flusk-lang — DO NOT EDIT

export interface DriftDetectedPayload {
  driftDetectionId: string;
  driftType: string;
  severity: string;
  score: number;
  organizationId: string;
}

export const publishDriftDetected = async (payload: DriftDetectedPayload): Promise<void> => {
  await fetch(process.env.WEBHOOK_URL, {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload),
  });
};

export const subscribeDriftDetected = async (): Promise<void> => {
  const maxAttempts = 3;
  // Subscribe to webhook channel and call dispatchAlert
  console.log('Subscribed to DriftDetected', { maxAttempts });
};
