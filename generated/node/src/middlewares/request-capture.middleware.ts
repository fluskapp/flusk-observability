// @generated by flusk-lang — DO NOT EDIT

import type { FastifyRequest, FastifyReply } from 'fastify';

/** Fastify onResponse hook — Capture LLM call data to SQLite for analysis */
export const requestCapture = async (request: FastifyRequest, reply: FastifyReply): Promise<void> => {
  const request = request;
  const response = response;
  const provider = detected?.provider;
  const costUsd = middleware?.cost-calculator?.costUsd;
  const promptHash = middleware?.duplicate-detector?.promptHash;
  const isDuplicate = middleware?.duplicate-detector?.isDuplicate;
  const latencyMs = computed?.latencyMs;
  // TODO: implement action "call" for step "capture"
  return [object Object];
  (request as Record<string, unknown>).middlewareOutput = { captured };
};
