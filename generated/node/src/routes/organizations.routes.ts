// @generated by flusk-lang — DO NOT EDIT

import { type FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import { findOrganizationById } from '../functions/find-organization-by-id.function.js';
import { listOrganizations } from '../functions/list-organizations.function.js';
import { createOrganization } from '../functions/create-organization.function.js';
import { updateOrganization } from '../functions/update-organization.function.js';
import { deleteOrganization } from '../functions/delete-organization.function.js';

interface AppWithDb extends FastifyInstance { db: Database.Database; }

export const organizationsRoutes = async (app: FastifyInstance): Promise<void> => {
  const { db } = app as AppWithDb;

  app.get('/api/organizations/me', async (request, reply) => {
    try {
      const result = findOrganizationById(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'findOrganizationById failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/organizations', async (request, reply) => {
    try {
      const result = listOrganizations(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'listOrganizations failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.post('/api/organizations', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = createOrganization(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'createOrganization failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/organizations/:id', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = findOrganizationById(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'findOrganizationById failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.put('/api/organizations/:id', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = updateOrganization(db, body);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'updateOrganization failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.delete('/api/organizations/:id', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = deleteOrganization(db, id);
      return reply.status(204).send();
    } catch (err: unknown) {
      request.log.error(err, 'deleteOrganization failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

};
