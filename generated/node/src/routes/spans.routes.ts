// @generated by flusk-lang — DO NOT EDIT

import { type FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import { listSpans } from '../functions/list-spans.function.js';
import { findSpanById } from '../functions/find-span-by-id.function.js';

interface AppWithDb extends FastifyInstance { db: Database.Database; }

export const spansRoutes = async (app: FastifyInstance): Promise<void> => {
  const { db } = app as AppWithDb;

  app.get('/api/spans', async (request, reply) => {
    try {
      const result = listSpans(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'listSpans failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/spans/:id', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = findSpanById(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'findSpanById failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

};
