// @generated by flusk-lang — DO NOT EDIT

import { type FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import { createOrganization } from '../functions/create-organization.function.js';
import { authenticateUser } from '../functions/authenticate-user.function.js';
import { validateApiKey } from '../functions/validate-api-key.function.js';

interface AppWithDb extends FastifyInstance { db: Database.Database; }

export const authRoutes = async (app: FastifyInstance): Promise<void> => {
  const { db } = app as AppWithDb;

  app.post('/auth/register', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = createOrganization(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'createOrganization failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.post('/auth/login', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = authenticateUser(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'authenticateUser failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.post('/auth/verify-key', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = validateApiKey(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'validateApiKey failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

};
