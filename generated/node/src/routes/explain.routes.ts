// @generated by flusk-lang — DO NOT EDIT

import { type FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import { explainWithAi } from '../functions/explain-with-ai.function.js';
import { generateExplainReport } from '../functions/generate-explain-report.function.js';

interface AppWithDb extends FastifyInstance { db: Database.Database; }

export const explainRoutes = async (app: FastifyInstance): Promise<void> => {
  const { db } = app as AppWithDb;

  app.post('/api/explain', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = explainWithAi(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'explainWithAi failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/explain/:sessionId', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = generateExplainReport(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'generateExplainReport failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

};
