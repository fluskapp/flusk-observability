// @generated by flusk-lang — DO NOT EDIT

import { type FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import { listAiUsageMetrics } from '../functions/listAiUsageMetrics.function.js';
import { createAiUsageMetric } from '../functions/createAiUsageMetric.function.js';
import { findAiUsageMetricById } from '../functions/findAiUsageMetricById.function.js';
import { deleteAiUsageMetric } from '../functions/deleteAiUsageMetric.function.js';
import { aggregateUsageByUser } from '../functions/aggregateUsageByUser.function.js';
import { aggregateUsageByTool } from '../functions/aggregateUsageByTool.function.js';
import { calculateUsageRank } from '../functions/calculateUsageRank.function.js';
import { calculateRoi } from '../functions/calculateRoi.function.js';

interface AppWithDb extends FastifyInstance { db: Database.Database; }

export const aiUsageRoutes = async (app: FastifyInstance): Promise<void> => {
  const { db } = app as AppWithDb;

  app.get('/api/ai-usage', async (request, reply) => {
    try {
      const result = listAiUsageMetrics(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'listAiUsageMetrics failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.post('/api/ai-usage', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = createAiUsageMetric(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'createAiUsageMetric failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/ai-usage/:id', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = findAiUsageMetricById(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'findAiUsageMetricById failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.delete('/api/ai-usage/:id', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = deleteAiUsageMetric(db, id);
      return reply.status(204).send();
    } catch (err: unknown) {
      request.log.error(err, 'deleteAiUsageMetric failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/ai-usage/by-user/:userId', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = aggregateUsageByUser(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'aggregateUsageByUser failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/ai-usage/by-tool/:toolName', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = aggregateUsageByTool(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'aggregateUsageByTool failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/ai-usage/rankings', async (request, reply) => {
    try {
      const result = calculateUsageRank(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'calculateUsageRank failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/ai-usage/roi', async (request, reply) => {
    try {
      const result = calculateRoi(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'calculateRoi failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

};
