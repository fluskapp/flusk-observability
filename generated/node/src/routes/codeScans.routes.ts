// @generated by flusk-lang — DO NOT EDIT

import { type FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import { listCodeScanResults } from '../functions/listCodeScanResults.function.js';
import { scanCodebase } from '../functions/scanCodebase.function.js';
import { findCodeScanResultById } from '../functions/findCodeScanResultById.function.js';
import { deleteCodeScanResult } from '../functions/deleteCodeScanResult.function.js';
import { generateScanReport } from '../functions/generateScanReport.function.js';

interface AppWithDb extends FastifyInstance { db: Database.Database; }

export const codeScansRoutes = async (app: FastifyInstance): Promise<void> => {
  const { db } = app as AppWithDb;

  app.get('/api/code-scans', async (request, reply) => {
    try {
      const result = listCodeScanResults(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'listCodeScanResults failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.post('/api/code-scans', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = scanCodebase(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'scanCodebase failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/code-scans/:id', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = findCodeScanResultById(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'findCodeScanResultById failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.delete('/api/code-scans/:id', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = deleteCodeScanResult(db, id);
      return reply.status(204).send();
    } catch (err: unknown) {
      request.log.error(err, 'deleteCodeScanResult failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/code-scans/:id/report', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = generateScanReport(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'generateScanReport failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

};
